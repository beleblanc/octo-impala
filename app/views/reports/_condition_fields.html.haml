%fieldset
  =f.attribute_fields do |a|
    =a.attribute_select associations: [:region,:status, :accuseds, :complainants, :constituency, :prosecutor, :judge,:charges,:user]
  =f.predicate_select
  =f.value_fields do |v|
    =v.text_field :value
  =link_to "remove", '#', class:"remove_search_fields btn btn-mini btn-danger"